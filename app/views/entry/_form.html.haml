= simple_form_for @entry, :html => { :class => 'form-horizontal' } do |f|
  = f.input :note_id
  = f.input :integer
  = f.input :author_id
  = f.input :weather
  = f.input :string
  = f.input :date
  = f.input :body
  = f.input :text
  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), entry_path, :class => 'btn'
